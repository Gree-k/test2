{% if article %}
    <div class="page-header">
        <h1> {{ article.title }} </h1>
    </div>
        <p class="lead"> {{ article.text }} </p>
        {% if article.user %}
            <p> Автор: {{ article.user.name }} </p>
        {% endif %}
{% endif %}
